name: hello-world
on: pull_request
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
          echo "sdsdds: ${{ github.head_ref }}.${{ github.sha }}"
          asdasdasadasd

